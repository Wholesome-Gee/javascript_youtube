extends ../base.pug
include ../mixins/video.pug

block contents
  div.profile__data
    img(src="/" + user.avatarUrl).avatar 
    h3=user.name
  div.video-grid
    each video in user.videos
      +video(video)
    else
      li 영상을 찾지 못했습니다.